<h2>New Question</h2>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for questions:</p>
    <ul>
      <li>Post a clear and direct question in the title.</li>
      <li>Bloccit will try to answer your questions in a timely manner.</li>
      <li>Seeking any identifying personal information, real or fake, will result in a ban without a prior warning.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @question do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter question title ..." %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Question content ..." %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
        <span style="float: right">
          <%= f.label "Resolved" %>
          <%= f.check_box :resolved %>
        </span>
      </div>
    <% end %>
  </div>
</div>