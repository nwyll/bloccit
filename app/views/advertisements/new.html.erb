<h2>New Advertisement</h2>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for advertisements:</p>
    <ul>
      <li>Selling your soul is frowned upon.</li>
      <li>The incessant use of Austin Powers references will get you banned.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @advertisement do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: 'form-control', placeholder: "Enter advertisement title ..." %>
      </div>
      <div class="form-group">
        <%= f.label :copy %>
        <%= f.text_area :copy, class: 'form-control', placeholder: "Advertisement content ..." %>
      </div>
      <div class="form-group">
        <%= f.label :price %>
        <%= f.text_field :price, class: 'form-control', placeholder: "Enter your asking price ... " %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: "btn btn-success" %>
      </div>
    <% end %>
</div>
